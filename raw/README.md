# Raw Sources

This directory is the source layer for the wiki.

- Put newly collected material directly in `raw/`.
- Move ingested files into `processed/`.
- Store downloaded images and attachments in `assets/`.

Agents may read these files but should not rewrite them in place.
